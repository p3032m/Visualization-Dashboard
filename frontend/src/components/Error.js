import React from 'react';

const Card = ({ imageSrc, text }) => {
  return (
    <div className='text-center flex flex-col mt-12 pt-5'>
        Error!! Page not found
      </div>
  );
};

export default Card;
